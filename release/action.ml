open Definition
open Constant
open Util

let handle_action g a=
  let rollD g = 
    let numRolled = random_roll ()in
    let rec add_resources (player, structures) =
      match 
     let robberRoll game =
     

  match a with
    | RollDice ->
    | MaritimeTrade(mt) ->
    | DomesticTrade(dt) ->
    | BuyBuild(b) ->
    | PlayCard(pc) ->
    | EndTurn ->
